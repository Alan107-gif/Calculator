# Calculator

This repository provides a simple visual calculator example written in C++ using Qt.

## Building

Ensure Qt5 or Qt6 development files are installed. Then build with CMake:

```bash
mkdir build && cd build
cmake ..
cmake --build .
```

